{# Fragment to load dynamically into ship info panel. #}
{{ include('partials/ship-nav-table.html.twig'
    , {nav: nav}) }}

{{ include('partials/ship-fuel-table.html.twig'
    , {fuel: fuel}) }}

{# TODO list events #}
